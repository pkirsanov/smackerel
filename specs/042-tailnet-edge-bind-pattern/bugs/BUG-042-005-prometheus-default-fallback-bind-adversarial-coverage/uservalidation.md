# User Validation: BUG-042-005 — Compose contract test missing adversarial coverage for literal-bind / default-fallback bind on prometheus

## Acceptance Checklist

- [x] **AC-1 verified:** new `TestComposeContract_AdversarialPrometheusLiteralBindAndFallbackForms` function exists in `internal/deploy/compose_contract_test.go` (declared at line 730, docstring opening at line 692). The function uses a table-driven sweep with exactly two sub-cases: `literal_127.0.0.1_bind_(spec_020_form)` (port = `127.0.0.1:${PROMETHEUS_HOST_PORT}:9090`) and `default-fallback_${HOST_BIND_ADDRESS:-127.0.0.1}_bind_(forbidden_by_Gate_G028)` (port = `${HOST_BIND_ADDRESS:-127.0.0.1}:${PROMETHEUS_HOST_PORT}:9090`). Validated GREEN by `go test -count=1 -v -run '^TestComposeContract_AdversarialPrometheusLiteralBindAndFallbackForms$' ./internal/deploy/...` showing `--- PASS:` for both sub-cases. (See [report.md > Targeted contract suite](report.md#targeted-contract-suite-full-canary--new).)
- [x] **AC-2 verified:** each sub-case carries two assertions: (1) `strings.Contains(err.Error(), "prometheus")` proves the rejection error mentions the violating service; (2) anchor-list `Contains` loop over `[spec 049, spec 042, fail-loud, ${HOST_BIND_ADDRESS:?, ${HOST_BIND_ADDRESS:-127.0.0.1}, literal 127.0.0.1:]` proves the rejection lands on the right contract surface (spec 042 + spec 049 + Gate G028) rather than some unrelated stub error. Captured `compose_contract_test.go:784: adversarial OK: ...` log lines in the GREEN run show the rejection error includes `spec 049 inherits the spec 042 tailnet-edge bind contract`, `fail-loud ${HOST_BIND_ADDRESS:?...}`, `no literal 127.0.0.1: prefix`, and `no default-fallback ${HOST_BIND_ADDRESS:-127.0.0.1} form` — multiple anchor terms present, confirming non-tautological assertion. (See [report.md > Code Diff Evidence](report.md#code-diff-evidence) line 782 grep hit.)
- [x] **AC-3 verified:** `grep -n 'HL-RESCAN-010' internal/deploy/compose_contract_test.go` returns multiple hits inside the new test function: line 697 docstring attribution `// Before HL-RESCAN-010, no synthetic adversarial test existed for prometheus`; line 725 docstring attribution `// Discovered: home-lab readiness re-scan finding HL-RESCAN-010 (P3)`; line 762 failure-case `t.Fatalf` attribution `HL-RESCAN-010 prometheus literal-bind / default-fallback coverage gap is reintroduced`; line 782 anchor-list assertion failure message attribution. A future maintainer hitting either the relax-then-FAIL-RED case or the assertion-failure case will see the `HL-RESCAN-010` breadcrumb pointing them back at this bug packet. (See [report.md > Code Diff Evidence](report.md#code-diff-evidence).)
- [x] **AC-4 verified:** `TestComposeContract_LiveFile` continues to PASS GREEN against the unchanged `deploy/compose.deploy.yml`. The targeted run includes `--- PASS: TestComposeContract_LiveFile (0.00s)`. The new test is purely additive: the live compose file already complies (the prometheus service uses `${HOST_BIND_ADDRESS:?...}:${PROMETHEUS_HOST_PORT}:` per spec 049's inheritance of the spec 042 contract), so the canary cannot regress as a side effect of this fix — and it doesn't. (See [report.md > Targeted contract suite](report.md#targeted-contract-suite-full-canary--new).)
- [x] **AC-5 verified:** all pre-existing adversarial tests continue to PASS GREEN unchanged: `TestComposeContract_AdversarialLiteralBind`, `TestComposeContract_AdversarialInfraHasPorts`, `TestComposeContract_AdversarialMultiPortsBypass`, `TestComposeContract_AdversarialMLMultiPortsBypass`, `TestComposeContract_AdversarialNetworkModeHostBypass` (5 sub-cases), `TestComposeContract_AdversarialOllamaLiteralBind` (2 sub-cases), `TestComposeContract_AdversarialDefaultFallbackBind` (3 sub-cases). Plus `TestVulnGateContract_*` (11 tests) and `TestBundleHashContract_*` (5 tests). Total: 9 `TestComposeContract_*` functions + 16 contract-canary functions all PASS — the new test does not over-reach into adjacent contract surfaces. (See [report.md > Canary suite](report.md#canary-suite).)
- [x] **AC-6 verified:** [report.md > Red→Green proof (scenario-first TDD)](report.md#redgreen-proof-scenario-first-tdd) captures both phases — RED: temporarily relaxing the prometheus branch of `assertComposeContract` from strict `strings.HasPrefix(p, requiredPrometheusPrefix)` to too-loose `strings.Contains(p, "${HOST_BIND_ADDRESS:")` causes the default-fallback sub-case to FAIL with `the contract is tautological — it would NOT catch a regression to the spec 020 literal form or to the default-fallback ${HOST_BIND_ADDRESS:-127.0.0.1} form for prometheus; HL-RESCAN-010 prometheus literal-bind / default-fallback coverage gap is reintroduced`; the literal-bind sub-case continues to PASS unchanged because the literal `127.0.0.1:` does not contain the `${HOST_BIND_ADDRESS:` substring — this is the **positive cross-check** that proves the new test correctly attributes failures to the specific regression vector under test (the relaxation specifically smuggles only the default-fallback form). GREEN: restoring the strict `strings.HasPrefix(p, requiredPrometheusPrefix)` check returns both sub-cases to PASS.

## Bounded-Scope Validation

- [x] **Single-file code change:** the only Go file modified is `internal/deploy/compose_contract_test.go` (+97 lines / 0 deletions per `git diff --shortstat HEAD`). No production runtime code, no compose files, no `config/smackerel.yaml`, no CI workflow, no scripts, no Dockerfile, no `ml/...`. (See [report.md > Code Diff Evidence](report.md#code-diff-evidence).)
- [x] **Foreign-owned spec content untouched:** zero edits to `specs/042-tailnet-edge-bind-pattern/spec.md`, `specs/042-tailnet-edge-bind-pattern/design.md`, `specs/042-tailnet-edge-bind-pattern/scopes.md`, `specs/042-tailnet-edge-bind-pattern/state.json`, `specs/042-tailnet-edge-bind-pattern/uservalidation.md`, `specs/042-tailnet-edge-bind-pattern/report.md`. Zero edits to `specs/049-*/`. The `bubbles.devops` mode artifact-ownership boundary is respected.
- [x] **Live file unchanged:** `deploy/compose.deploy.yml` is bit-identical to HEAD. The fix locks compliance against future drift; it does not require the live file to change. The pre-existing `TestComposeContract_LiveFile` canary continues to PASS GREEN against the unchanged file.
- [x] **`assertComposeContract` and constants untouched:** zero edits to the `assertComposeContract` function body, zero edits to `requiredCorePrefix` / `requiredMLPrefix` / `requiredPrometheusPrefix` / `requiredOllamaPrefix` constants. The fix is purely additive at the test layer; the runtime behavior of the existing assertion is unchanged.
- [x] **Cross-package smoke clean:** `./smackerel.sh test unit --go` returns `[go-unit] go test ./... finished OK` — every `internal/*` and `cmd/*` package passes with `ok`. No regression in spec 042 BUG-042-001 + BUG-042-002 + BUG-042-003 + BUG-042-004 contracts, the SST-loader contract (spec 044), or the per-user bearer auth contract. (See [report.md > Cross-package smoke](report.md#cross-package-smoke).)
- [x] **Static checks clean:** `go vet ./internal/deploy/...` exit 0; `gofmt -l internal/deploy/` empty. (See [report.md > Static checks](report.md#static-checks).)
- [x] **Canary suite preserved:** all nine pre-existing `TestComposeContract_*` adversarial tests (literal-bind, infra-has-ports, multi-ports core/ml, network_mode 5 sub-cases, ollama literal/default-fallback, default-fallback core/ml + ml-literal) plus `TestComposeContract_LiveFile`, `TestVulnGateContract_LiveFile` + 10 sub-tests, and `TestBundleHashContract_LiveFile` + 4 sub-tests all PASS unchanged. (See [report.md > Canary suite](report.md#canary-suite).)
- [x] **Generic-only constraint preserved:** zero real hostnames, IPs, tailnet identifiers, operator-specific topology, or PII in any of the seven packet artifacts. Only `127.0.0.1` loopback and `${HOST_BIND_ADDRESS:?...}` SST substitution form — both repo-policy-allow-listed.
- [x] **Terminal discipline preserved:** all file edits via IDE tools (`replace_string_in_file`, `create_file`). Zero shell redirection at any point in the implementation or in the RED→GREEN proof.
- [x] **Rollback path verified:** single `git revert` of the BUG-042-005 commit. The new test is purely additive at the test layer (no production runtime / live compose / constant declaration changes); revert is risk-free. Verified by the RED proof itself, which performs an in-place relaxation, captures expected output, and restores via `replace_string_in_file`.

## Sign-off

**Status: SHIP_IT**

The fix closes home-lab readiness re-scan finding **HL-RESCAN-010** (P3). The spec 042 / spec 049 / Gate G028 compose contract surface is now mechanically locked against accidental relaxation of the strict `strings.HasPrefix` prefix check on `prometheus` — completing the four-service coverage matrix (smackerel-core, smackerel-ml, ollama, prometheus) where every operator-facing service in the compose file has both a positive canary AND adversarial sub-tests covering the literal-bind and default-fallback regression vectors. A future regression that softens the prometheus branch (e.g., to too-loose `strings.Contains` substring matching) will FAIL RED at the `unit-tests` gate before merge. The live `deploy/compose.deploy.yml` already complies with the contract, so the fix is risk-free to ship.
