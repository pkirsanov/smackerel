# User Validation: BUG-042-004 — Compose contract test missing adversarial coverage for default-fallback bind on smackerel-core/ml

## Acceptance Checklist

- [x] **AC-1 verified:** new `TestComposeContract_AdversarialDefaultFallbackBind/smackerel-core_uses_${HOST_BIND_ADDRESS:-127.0.0.1}:_default-fallback_bind_(forbidden_by_Gate_G028)` sub-case exists in `internal/deploy/compose_contract_test.go` (declared inside the function at line 611, table-driven case anchor-list assertion at line 685). The sub-case builds a compose YAML fixture where `services.smackerel-core.ports[0] = "${HOST_BIND_ADDRESS:-127.0.0.1}:${CORE_HOST_PORT}:${CORE_CONTAINER_PORT}"` and asserts `assertComposeContract` returns a non-nil error mentioning `smackerel-core` AND at least one of the anchor terms `[spec 020, ${HOST_BIND_ADDRESS:-127.0.0.1}, Gate-G028, fail-loud]`. Validated GREEN by `go test -count=1 -v -run '^TestComposeContract_AdversarialDefaultFallbackBind$' ./internal/deploy/...` showing `--- PASS:` for the smackerel-core sub-case. (See [report.md > Targeted contract suite](report.md#targeted-contract-suite-full-canary--new).)
- [x] **AC-2 verified:** sibling `TestComposeContract_AdversarialDefaultFallbackBind/smackerel-ml_uses_${HOST_BIND_ADDRESS:-127.0.0.1}:_default-fallback_bind_(forbidden_by_Gate_G028)` sub-case covers the smackerel-ml branch with the analogous fixture. Validated GREEN by the same `go test` invocation showing `--- PASS:` for the ml sub-case. (See [report.md > Targeted contract suite](report.md#targeted-contract-suite-full-canary--new).)
- [x] **AC-3 verified:** sibling `TestComposeContract_AdversarialDefaultFallbackBind/smackerel-ml_uses_literal_127.0.0.1:_bind_(spec_020_form)` sub-case covers the spec 020 literal-bind regression on the ml branch (which was previously uncovered — only smackerel-core literal-bind had a guard via `TestComposeContract_AdversarialLiteralBind`). Validated GREEN by the same `go test` invocation showing `--- PASS:` for the ml literal sub-case. (See [report.md > Targeted contract suite](report.md#targeted-contract-suite-full-canary--new).)
- [x] **AC-4 verified:** each sub-case carries two assertions: (1) `strings.Contains(err.Error(), tc.service)` proves the rejection error mentions `smackerel-core` or `smackerel-ml` (the violating service); (2) anchor-list `Contains` loop over `[spec 020, ${HOST_BIND_ADDRESS:-127.0.0.1}, Gate-G028, fail-loud]` proves the rejection lands on the right contract surface (spec 042 / Gate G028) rather than some unrelated stub error. Captured `compose_contract_test.go:687: adversarial OK: ...` log lines in the GREEN run show the rejection error includes `spec 020 form`, `${HOST_BIND_ADDRESS:-127.0.0.1}`, `Gate G028 NO-DEFAULTS`, and `fail-loud ${HOST_BIND_ADDRESS:?...}` — all four anchor terms present, confirming non-tautological assertion. (See [report.md > Code Diff Evidence](report.md#code-diff-evidence) line 685 grep hit.)
- [x] **AC-5 verified:** `grep -n 'HL-RESCAN-009' internal/deploy/compose_contract_test.go` returns multiple hits inside the new test function: line 608 docstring attribution `// Discovered: home-lab readiness re-scan finding HL-RESCAN-009 (P3)`; line 665 failure-case `t.Fatalf` attribution `HL-RESCAN-009 default-fallback / literal-bind ml-side coverage gap is reintroduced`; line 673 inline rationale comment; line 685 anchor-list assertion failure message attribution. A future maintainer hitting either the relax-then-FAIL-RED case or the assertion-failure case will see the `HL-RESCAN-009` breadcrumb pointing them back at this bug packet. (See [report.md > Code Diff Evidence](report.md#code-diff-evidence).)
- [x] **AC-6 verified:** [report.md > Red→Green proof (scenario-first TDD)](report.md#redgreen-proof-scenario-first-tdd) captures both phases — RED: temporarily relaxing the smackerel-core branch of `assertComposeContract` from strict `strings.HasPrefix(p, requiredCorePrefix)` to too-loose `strings.Contains(p, "${HOST_BIND_ADDRESS:")` causes the smackerel-core sub-case to FAIL with `the contract is tautological — it would NOT catch a regression to ${HOST_BIND_ADDRESS:-127.0.0.1}: default-fallback or literal 127.0.0.1: form; HL-RESCAN-009 default-fallback / literal-bind ml-side coverage gap is reintroduced`; the two smackerel-ml sub-cases continue to PASS unchanged because the relaxation only touched the smackerel-core branch — this is the **positive cross-check** that proves the new test correctly attributes failures to the specific service branch under test. GREEN: restoring the strict `strings.HasPrefix(p, requiredCorePrefix)` check returns all 3 sub-cases to PASS.

## Bounded-Scope Validation

- [x] **Single-file code change:** the only Go file modified is `internal/deploy/compose_contract_test.go` (+112 lines / 0 deletions per `git diff --stat`). No production runtime code, no compose files, no `config/smackerel.yaml`, no CI workflow, no scripts, no Dockerfile, no `ml/...`. (See [report.md > Code Diff Evidence](report.md#code-diff-evidence).)
- [x] **Foreign-owned spec content untouched:** zero edits to `specs/042-tailnet-edge-bind-pattern/spec.md`, `specs/042-tailnet-edge-bind-pattern/design.md`, `specs/042-tailnet-edge-bind-pattern/scopes.md`, `specs/042-tailnet-edge-bind-pattern/state.json`, `specs/042-tailnet-edge-bind-pattern/uservalidation.md`, `specs/042-tailnet-edge-bind-pattern/report.md`. The `bubbles.devops` mode artifact-ownership boundary is respected.
- [x] **Live file unchanged:** `deploy/compose.deploy.yml` is bit-identical to HEAD. The fix locks compliance against future drift; it does not require the live file to change. The pre-existing `TestComposeContract_LiveFile` canary continues to PASS GREEN against the unchanged file.
- [x] **`assertComposeContract` and constants untouched:** zero edits to the `assertComposeContract` function body, zero edits to `requiredCorePrefix` / `requiredMLPrefix` / `requiredPrometheusPrefix` / `requiredOllamaPrefix` constants. The fix is purely additive at the test layer; the runtime behavior of the existing assertion is unchanged.
- [x] **Cross-package smoke clean:** `./smackerel.sh test unit --go` returns `[go-unit] go test ./... finished OK` — every `internal/*` and `cmd/*` package passes with `ok`. No regression in spec 042 BUG-042-001 + BUG-042-002 + BUG-042-003 contracts, the SST-loader contract (spec 044), or the per-user bearer auth contract. (See [report.md > Cross-package smoke](report.md#cross-package-smoke).)
- [x] **Static checks clean:** `go vet ./internal/deploy/...` exit 0; `gofmt -l internal/deploy/` empty. (See [report.md > Static checks](report.md#static-checks).)
- [x] **Canary suite preserved:** all eight pre-existing `TestComposeContract_*` adversarial tests (literal-bind, infra-has-ports, multi-ports core/ml, network_mode 5 sub-cases, ollama literal/default-fallback) plus `TestComposeContract_LiveFile`, `TestVulnGateContract_LiveFile`, and `TestBundleHashContract_*` all PASS unchanged. (See [report.md > Canary suite](report.md#canary-suite).)
- [x] **Generic-only constraint preserved:** zero real hostnames, IPs, tailnet identifiers, operator-specific topology, or PII in any of the seven packet artifacts. Only `127.0.0.1` loopback and `${HOST_BIND_ADDRESS:?...}` SST substitution form — both repo-policy-allow-listed.
- [x] **Terminal discipline preserved:** all file edits via IDE tools (`replace_string_in_file`, `create_file`). Zero shell redirection at any point in the implementation or in the RED→GREEN proof.
- [x] **Rollback path verified:** single `git revert` of the BUG-042-004 commit. The new test is purely additive at the test layer (no production runtime / live compose / constant declaration changes); revert is risk-free. Verified by the RED proof itself, which performs an in-place relaxation, captures expected output, and restores via `replace_string_in_file`.

## Sign-off

**Status: SHIP_IT**

The fix closes home-lab readiness re-scan finding **HL-RESCAN-009** (P3). The spec 042 / Gate G028 compose contract surface is now mechanically locked against accidental relaxation of the strict `strings.HasPrefix` prefix check on `smackerel-core` AND `smackerel-ml`. A future regression that softens either branch (e.g., to too-loose `strings.Contains` substring matching) will FAIL RED at the `unit-tests` gate before merge. The live `deploy/compose.deploy.yml` already complies with the contract, so the fix is risk-free to ship.
