# User Validation: BUG-044-001 — auth revocation broadcaster falls back to literal `"smackerel-core"` when HOSTNAME is empty

## Acceptance Checklist

- [x] **AC-1 verified:** `cmd/core/wiring.go` no longer contains the silent-fallback form `os.Getenv("HOSTNAME")` followed by `if instanceID == "" { instanceID = "smackerel-core" }` in any executable code branch. The HOSTNAME read at the broadcaster wiring site goes through the new `resolveBroadcasterInstanceID()` helper. The only remaining occurrence of the pre-fix literal-fallback string is inside the explanatory comment block at line 262 (archaeology). (See report.md > Code Diff Evidence > "Confirmation that the pre-fix executable form is gone".)
- [x] **AC-2 verified:** `cmd/core/wiring.go` line 91 declares a package-private helper `resolveBroadcasterInstanceID() (string, error)` placed before `buildAPIDeps`. The helper returns `(hostname, nil)` on the happy path and `("", error)` when HOSTNAME is empty. The error message names HOSTNAME, HL-RESCAN-008, Gate G028, spec 044, AND deduplication — confirmed by the `Empty_FailsLoud` test's 5-anchor-token `strings.Contains` assertion loop at line 64 of the test file. (See report.md > Code Diff Evidence > grep output for `func resolveBroadcasterInstanceID`.)
- [x] **AC-3 verified:** When `resolveBroadcasterInstanceID()` returns a non-nil error, the broadcaster construction block at line 272 emits `slog.Error("auth revocation broadcaster construction refused", "error", hostnameErr, "subject", cfg.Auth.RevocationNATSSubject)` and skips `revocation.NewBroadcaster` construction entirely. The block does not panic or `os.Exit` — matching the existing non-fatal-but-loud handling in the same block where `revocation.NewBroadcaster` construction errors and `Subscribe` errors are also non-fatal. (See report.md > Code Diff Evidence > grep output for `auth revocation broadcaster`.)
- [x] **AC-4 verified:** When `resolveBroadcasterInstanceID()` returns a non-nil string with nil error, the broadcaster construction block at line 276 invokes `revocation.NewBroadcaster(svc.nc.Conn, cfg.Auth.RevocationNATSSubject, revocationCache, instanceID)` exactly as before — preserved unchanged from the pre-fix block. The existing non-fatal handling for construction errors (line 279) and subscribe errors (line 282) is preserved. (See report.md > Code Diff Evidence > grep output for `revocation.NewBroadcaster`.)
- [x] **AC-5 verified:** `cmd/core/wiring_revocation_test.go` (new file, 95 lines) declares 3 test methods at lines 33, 51, 76: `TestResolveBroadcasterInstanceID_NonEmpty` (positive guard rail), `TestResolveBroadcasterInstanceID_Empty_FailsLoud` (adversarial — empty-string set), and `TestResolveBroadcasterInstanceID_UnsetEnv` (adversarial — env var unset). All 3 PASS GREEN in 35ms. (See report.md > Code Diff Evidence > grep output for `^func TestResolveBroadcasterInstanceID` + Validation Evidence > Targeted Go-driver run.)
- [x] **AC-6 verified:** RED proof captured by temporarily reverting the helper body to a silent-fallback form (`return "smackerel-core", nil` on empty) via `replace_string_in_file`: exactly TWO of the three tests FAIL (`Empty_FailsLoud`, `UnsetEnv`) with the explicit `wiring_revocation_test.go:58: expected non-nil error for empty HOSTNAME (HL-RESCAN-008 silent fallback regression), got id="smackerel-core" err=nil` and `wiring_revocation_test.go:90: expected non-nil error for unset HOSTNAME, got id="smackerel-core" err=nil` mismatch messages; the positive `NonEmpty` test continues to PASS. Restoration via `replace_string_in_file` returns all 3 tests to PASS GREEN. (See report.md > Test Evidence > Red→Green proof (scenario-first TDD).)
- [x] **AC-7 verified:** The full cmd/core/ Go unit suite (`go test -count=1 ./cmd/core/...`) PASSES GREEN in 401ms — every test in the cmd/core package including the pre-existing `TestAllConnectorsRegistered` (15-connector registration contract) plus the 3 new helper tests. The full repo-CLI Go unit lane (`./smackerel.sh test unit --go` → `go test ./...`) returns `[go-unit] go test ./... finished OK`. Zero regression in any pre-existing test. (See report.md > Validation Evidence > Cross-test smoke + Repo-CLI smoke.)
- [x] **AC-8 verified:** Generic-only constraint preserved: zero real hostnames, IPs, tailnet identifiers, owner-username tokens, or operator-specific topology introduced into any source file or evidence block. The test fixture's HOSTNAME value is a synthetic literal (`"smackerel-core-replica-7"`) chosen to be obviously non-real. The error-message anchor tokens (`HOSTNAME`, `HL-RESCAN-008`, `Gate G028`, `spec 044`, `deduplication`) are policy / finding / spec identifiers, not operator-specific values. (See report.md > Constraint Adherence.)

## Bounded-Scope Validation

- [x] **Two-file code change:** the only files modified are `cmd/core/wiring.go` (production — helper added before `buildAPIDeps` + broadcaster construction block rewritten as nested-switch, +47/-14 lines) and `cmd/core/wiring_revocation_test.go` (new file — 95 lines, 3 test methods). No production runtime Python code, no compose files, no `config/smackerel.yaml`, no SST loader script, no CI workflow, no Dockerfile, no `internal/auth/revocation/broadcaster.go`, no other cmd/core files (`main.go`, `services.go`, `cmd_*.go`, `helpers.go`, `wiring_agent.go`, etc.). (See report.md > Code Diff Evidence > git diff stat output + git status output.)
- [x] **Foreign-owned spec content untouched:** zero edits to `specs/044-per-user-bearer-auth/spec.md`, `design.md`, `scopes.md`, `state.json`, `uservalidation.md`, `report.md`. The `bubbles.devops` mode artifact-ownership boundary is respected.
- [x] **SST source-of-truth preserved:** `config/smackerel.yaml` is unchanged. The SST loader `scripts/commands/config.sh` is unchanged. `HOSTNAME` is a Docker runtime env var injected by the container orchestrator (not an SST-managed value), so no SST regeneration is needed. (See report.md > Code Diff Evidence > git status output for excluded surfaces.)
- [x] **Cross-test smoke clean:** `./smackerel.sh test unit --go` returns `[go-unit] go test ./... finished OK` — zero regression in any pre-existing test, including the spec 044 broadcaster integration tests (`tests/integration/auth_revocation_test.go`, `tests/integration/auth_chaos_*_test.go`) which construct the Broadcaster with synthetic instance IDs and are unaffected by this wiring-site change. (See report.md > Audit Evidence > Cross-package smoke.)
- [x] **Static checks clean:** `go vet ./cmd/core/... ./internal/auth/...` exits 0 — no shadow, no nil-deref, no struct-tag issues introduced. (See report.md > Validation Evidence > Static checks.)
- [x] **Generic-only constraint preserved:** zero real hostnames, IPs, tailnet identifiers, operator-specific topology, or PII. All references use SST substitution forms or generic synthetic placeholders. (See report.md > Constraint Adherence.)
- [x] **Terminal discipline preserved:** all file edits via IDE tools (`replace_string_in_file`, `multi_replace_string_in_file`, `create_file`). The RED→GREEN proof toggle used `replace_string_in_file` for both the revert and the restore. Zero shell redirection at any point in the implementation or in the RED→GREEN proof. (See report.md > Constraint Adherence.)
- [x] **Repo-CLI bypass avoided:** every `**Command:**` entry uses the repo CLI (`./smackerel.sh test unit --go`); raw `go test` invocations only appear as the underlying tool the repo CLI delegates to, captured for human-readable evidence. (See report.md > Constraint Adherence.)
- [x] **Adversarial coverage non-tautological:** the two FAILs in the RED state cannot be satisfied by the pre-fix code: (a) the `Empty_FailsLoud` test asserts `err != nil` with the 5-anchor-token check — pre-fix code returns `("smackerel-core", nil)` with `err=nil`, never the non-nil error the test asserts; (b) the `UnsetEnv` test exercises the genuinely-unset branch via `os.Unsetenv("HOSTNAME")` with cleanup restore — pre-fix `os.Getenv` returns `""` for unset and the silent fallback yields the same `("smackerel-core", nil)` mismatch. The positive `NonEmpty` test passes on both pre-fix and post-fix code by coincidence (its setup matches what pre-fix code happens to do for the happy path). (See report.md > Test Evidence > Red→Green proof > Adversarial isolation.)

## Sign-off

**Status: SHIP_IT**

The fix closes self-hosted readiness re-scan finding HL-RESCAN-008 (P2). The auth-revocation broadcaster's per-replica instance-ID read is now mechanically locked to the Gate G028 fail-loud read pattern via the package-private `resolveBroadcasterInstanceID()` helper — a regression to the silent-fallback form (returning a literal `"smackerel-core"` on empty HOSTNAME), to the missing-attribution form (dropping any of the 5 anchor tokens from the error message), or to a future `os.LookupEnv` refactor that mishandles the unset branch would be caught at pre-merge by the new `TestResolveBroadcasterInstanceID_*` adversarial tests in `cmd/core/wiring_revocation_test.go`. The self-hosted `cfg.Auth.RevocationNATSSubject` is unset in the canonical config today, so the broadcaster wiring block does not even run in production — the fix is risk-free to ship; the runtime behavior is unchanged for the canonical self-hosted deployment, and the bypass of the spec 044 / Gate G028 SST gate at the wiring site is closed. Rollback is a single git revert of the BUG-044-001 commit; no live-config mismatch could result from a revert.
