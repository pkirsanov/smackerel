# User Validation: BUG-043-003 — ML OCR fallback reads OLLAMA_URL with silent default, bypassing the spec 043 ENABLE_OLLAMA SST gate

## Acceptance Checklist

- [x] **AC-1 verified:** `ml/app/ocr.py` line 244 reads `os.environ["ENABLE_OLLAMA"].strip().lower()` (fail-loud, no positional default to `os.environ.get` or `os.getenv`). The pre-fix `os.environ.get("OLLAMA_URL", "")` form is removed from the executable code; the only remaining occurrence is inside the explanatory comment block above the new gate. (See report.md > Code Diff Evidence.)
- [x] **AC-2 verified:** When `ENABLE_OLLAMA` is truthy (`true`/`1`/`yes`/`on`, case-insensitive, whitespace-trimmed), `handle_ocr_request` invokes `extract_text_ollama(image_bytes)` which itself reads `OLLAMA_URL` fail-loud at line 91. `TestEnableOllamaFailLoudGating::test_enable_ollama_truthy_invokes_ollama_fallback` PASSES GREEN. (See report.md > Validation Evidence > Targeted Python-driver run.)
- [x] **AC-3 verified:** When `ENABLE_OLLAMA` is falsy (`false`/`0`/`no`/`off`/`""`, case-insensitive, whitespace-trimmed), `handle_ocr_request` skips the Ollama fallback entirely and returns `ocr_engine="tesseract"`. `TestEnableOllamaFailLoudGating::test_enable_ollama_falsy_skips_ollama_fallback` PASSES GREEN. (See report.md > Validation Evidence > Targeted Python-driver run.)
- [x] **AC-4 verified:** When `ENABLE_OLLAMA` is missing entirely from the environment, `handle_ocr_request` raises `KeyError` naming `ENABLE_OLLAMA` on the slow path. `TestEnableOllamaFailLoudGating::test_enable_ollama_unset_raises_keyerror` PASSES GREEN. (See report.md > Validation Evidence > Targeted Python-driver run.)
- [x] **AC-5 verified:** When `ENABLE_OLLAMA` is set to a non-canonical token (e.g. `maybe`), `handle_ocr_request` raises `RuntimeError` mentioning `ENABLE_OLLAMA must be exactly one of` AND naming the invalid value AND naming `Gate G028`. `TestEnableOllamaFailLoudGating::test_enable_ollama_invalid_value_raises_runtimeerror` PASSES GREEN. (See report.md > Validation Evidence > Targeted Python-driver run + Code Diff Evidence > grep output for `RuntimeError`.)
- [x] **AC-6 verified:** `ENABLE_OLLAMA` is only consulted on the slow path (inside `if len(text) < MIN_TESSERACT_CHARS:`); the fast path does not read the variable. `TestEnableOllamaFailLoudGating::test_enable_ollama_only_consulted_when_tesseract_insufficient` PASSES GREEN — the test omits `ENABLE_OLLAMA` from the env via `clear=True` and asserts the function returns successfully when Tesseract output is sufficient (proving the gate was not consulted). (See report.md > Validation Evidence > Targeted Python-driver run.)
- [x] **AC-7 verified:** `ml/tests/test_ocr.py` declares a new `TestEnableOllamaFailLoudGating` class with 5 test methods (truthy / falsy / unset / invalid / lazy-gating). All 5 PASS in GREEN state. (See report.md > Code Diff Evidence > grep output for `class TestEnableOllamaFailLoudGating`.)
- [x] **AC-8 verified:** RED proof captured by temporarily reverting `ml/app/ocr.py` lines 244–256 to the pre-fix `os.environ.get("OLLAMA_URL", "")` form via `replace_string_in_file`: exactly THREE sub-tests FAIL (`test_enable_ollama_truthy_invokes_ollama_fallback`, `test_enable_ollama_unset_raises_keyerror`, `test_enable_ollama_invalid_value_raises_runtimeerror`) with the expected error messages; every other test PASSES (442 passed, 3 failed). Restoring the fix returns the suite to all-PASS GREEN (445 passed). (See report.md > Test Evidence > Red→Green proof (scenario-first TDD).)

## Bounded-Scope Validation

- [x] **Three-file code change:** the only files modified are `ml/app/ocr.py` (production), `ml/tests/test_ocr.py` (new test class), and `ml/tests/test_keep.py` (minimal `patch.dict(os.environ, ...)` updates to two pre-existing tests). No production runtime Go code, no compose files, no `config/smackerel.yaml`, no SST loader script, no CI workflow, no Dockerfile, no other Python ML sidecar modules. (See report.md > Code Diff Evidence > git diff stat output.)
- [x] **Foreign-owned spec content untouched:** zero edits to `specs/043-ollama-test-infrastructure/spec.md`, `design.md`, `scopes.md`, `state.json`, `uservalidation.md`, `report.md`. The `bubbles.devops` mode artifact-ownership boundary is respected.
- [x] **SST source-of-truth preserved:** the spec 043 `environments.<env>.ollama_enabled` per-env keys in `config/smackerel.yaml` are unchanged. The home-lab default (`ollama_enabled: false`) is unchanged. The SST loader `scripts/commands/config.sh` is unchanged. (See report.md > Code Diff Evidence > git status output for excluded surfaces.)
- [x] **Cross-test smoke clean:** `./smackerel.sh test unit --python` returns `445 passed in 12.79s` — zero regression in any pre-existing test, including the spec 043 BUG-001 + BUG-002 surfaces (compose / Dockerfile, untouched by this fix), the keep-connector OCR contract, and the SSRF protection suite. (See report.md > Audit Evidence > Cross-package smoke.)
- [x] **Static checks clean:** `./smackerel.sh lint` Python lint stage exits 0; no formatter drift. (See report.md > Validation Evidence > Static checks.)
- [x] **Generic-only constraint preserved:** zero real hostnames, IPs, tailnet identifiers, operator-specific topology, or PII. All references use SST substitution forms or generic placeholders. (See report.md > Constraint Adherence.)
- [x] **Terminal discipline preserved:** all file edits via IDE tools (`replace_string_in_file`, `multi_replace_string_in_file`, `create_file`). Zero shell redirection at any point in the implementation or in the RED→GREEN proof. (See report.md > Constraint Adherence.)
- [x] **Repo-CLI bypass avoided:** every `**Command:**` entry uses the repo CLI (`./smackerel.sh test unit --python`, `./smackerel.sh lint`); raw `pytest` invocations only appear as the underlying tool the repo CLI delegates to, captured for human-readable evidence. (See report.md > Constraint Adherence.)
- [x] **Adversarial coverage non-tautological:** the three FAILs in the RED state cannot be satisfied by the pre-fix code: (a) the `truthy` test's `ollama_url_seen == [""]` assertion can only hold if `extract_text_ollama` is called with NO positional arg — pre-fix code passed one; (b) the `unset` test's `pytest.raises(KeyError)` can only hold if production code uses `os.environ[KEY]` — pre-fix `.get(KEY, "")` returns `""` and never raises; (c) the `invalid` test's `pytest.raises(RuntimeError)` can only hold if production code has a validation branch — pre-fix code has none. (See report.md > Test Evidence > Red→Green proof > Adversarial isolation.)

## Sign-off

**Status: SHIP_IT**

The fix closes home-lab readiness re-scan finding HL-RESCAN-006 (P2). The ML OCR fallback gate is now mechanically locked to the spec 043 `ENABLE_OLLAMA` SST flag, fail-loud per Gate G028 — a regression to the silent-default form (`os.environ.get("KEY", "default")`), the eager-gate-hoisting form (reading the var on every request), the missing-validation form (no strict-token check), or the missing-positional-arg-removal form (passing `ollama_url` positionally) would be caught at pre-merge by the new `TestEnableOllamaFailLoudGating` adversarial test class. The home-lab `config/smackerel.yaml` `environments.home-lab.ollama_enabled: false` already produces a generated env file with `ENABLE_OLLAMA=false`, so the fix is risk-free to ship — the runtime behavior is unchanged for the canonical home-lab deployment, and the bypass of the spec 043 SST gate is closed.
