{
    "id": "BUG-026-007-qwen3-thinking-blows-domain-budget",
    "title": "qwen3 default thinking-mode adds a ~113s hidden reasoning block to structured-JSON extraction, blowing the 30s domain-extraction budget into the degraded fallback",
    "severity": "high",
    "source": "redteam-adversarial-interrogation (live <deploy-host> prod, both models WARM)",
    "owningSpec": "026-domain-extraction",
    "workflowMode": "bugfix-fastlane",
    "status": "fixed_in_repo",
    "certification": {
        "status": "in_repo_verified",
        "note": "MECHANISM CORRECTED. The first fix (the /no_think prompt-injection token) was INEFFECTIVE — qwen3's Ollama chat template ignores the /no_think text directive and honors ONLY the native `think` request field (measured live on <deploy-host>: /no_think -> thinking STILL on, >150s, invalid-JSON; native think=False -> thinking OFF, ~1s compute). Reworked ollama_thinking.py to set the native litellm `think=False` kwarg (verified forwarded to Ollama /api/chat top-level by the sidecar-pinned litellm 1.84.0 via a request-capture probe) and migrated the two legacy ollama/ routes (search-rerank, drive-classify) to ollama_chat/. Reworked unit tests are adversarial: RED 9-fail with the mutator neutralized -> GREEN 556-pass restored (./smackerel.sh test unit --python). Live 'domain+synthesis fast + valid JSON' is only provable after the orchestrator rebuilds + signs + redeploys smackerel-ml on <deploy-host> — live verification PENDING redeploy."
    },
    "execution": {
        "activeAgent": "bubbles.bug",
        "currentPhase": "implement"
    },
    "completedPhases": [
        "discovery",
        "documentation",
        "analysis",
        "implement",
        "test"
    ],
    "completedScopes": [],
    "createdAt": "2026-07-09",
    "fixedAt": "2026-07-09",
    "lastUpdatedAt": "2026-07-09T00:00:00Z",
    "redeployRequired": true,
    "transitionRequests": [],
    "reworkQueue": [],
    "routed": [],
    "notes": "Remaining half of the redteam F2 (LLM-enrichment latency) finding. The keep_alive fix (BUG-026-006 sibling + ml/app/ollama_keepalive.py) removed the ~22-45s Ollama cold-load; this fixes the qwen3 hidden-reasoning latency on the structured-JSON extraction path by setting the native Ollama think=False request field (SST-gated by ML_STRUCTURED_EXTRACTION_THINKING / services.ml.structured_extraction_thinking) on every in-scope structured call, and migrating the two legacy ollama/ routes (search-rerank, drive-classify) to ollama_chat/. CORRECTION: the first fix used the /no_think prompt token, which qwen3 ignores; the SST switch + wiring are unchanged. The agent reasoning path (agent.py) is deliberately left thinking-ON (quality > latency). Adjacent finding (out of scope): _handle_generate_digest still carries its own inline /no_think, likely also ineffective on qwen3 — flagged for a follow-up bug. Requires a smackerel-ml image rebuild + signed redeploy to take effect."
}