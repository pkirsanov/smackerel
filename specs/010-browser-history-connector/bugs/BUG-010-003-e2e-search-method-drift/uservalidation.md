# User Validation Checklist

## Checklist

- [x] Bug packet exists for browser-history E2E search method drift.
- [ ] Browser-history E2E search uses the supported live API method and returns a successful response.
- [ ] The regression catches any return to GET `/api/search` in browser-history E2E search.
- [ ] Broad E2E no longer reports the browser-history `/api/search` method mismatch.
