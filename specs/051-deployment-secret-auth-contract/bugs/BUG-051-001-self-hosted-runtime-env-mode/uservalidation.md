# User Validation: BUG-051-001 — self-hosted config bundle emits SMACKEREL_ENV=development

## Checklist

- [x] **The defect (self-hosted runtime-mode bypass) is real.** Empirically reproduced via the `bash scripts/commands/config_self_hosted_runtime_env_test.sh` shell driver after reverting only the `self-hosted)` arm of the per-target case in `scripts/commands/config.sh`: Sub-test 1 FAILED with `actual: SMACKEREL_ENV=development` while Sub-tests 2/3/4 continued to PASS — exactly the pre-fix behavior. Re-applying the arm makes all four sub-tests PASS.
- [x] **The fix is the minimum viable change.** Single edit to `scripts/commands/config.sh`: structural transformation of the existing single-arm `if [[ "$TARGET_ENV" == "test" ]]; then SMACKEREL_ENV="test"; fi` form into a per-target `case "$TARGET_ENV" in test) ... ;; self-hosted) SMACKEREL_ENV="production" ;; esac` form, plus a 20-line comment block expansion explaining the BUG-051-001 / SEC-HL-001 rationale and the downstream consumers. Plus two new test files (one shell driver mirroring `config_secret_rejection_test.sh`, one thin Go driver mirroring `sst_loader_test.go`). No production runtime code, no compose, no `config/smackerel.yaml`, no doc files modified.
- [x] **The pre-existing FR-051-005 generator-side guard still fires.** `TestSSTLoader_RejectsDevPostgresPassword_SelfHosted` continues to PASS — the dev-default Postgres password rejection at the SST loader is intact. BUG-051-001 and FR-051-005 are orthogonal contracts; both fire on a self-hosted bundle that uses the dev-default Postgres password.
- [x] **The four new adversarial sub-tests are non-tautological.** The empirical RED state (Sub-test 1 FAIL when self-hosted arm is reverted, Sub-tests 2/3/4 PASS) proves the test isolates the regression contract from the orthogonal dev / test / FR-051-005 contracts. A future edit that drops the self-hosted arm fails Sub-test 1 with the explicit "BUG-051-001 reintroduced" message.
- [x] **The fix preserves the existing 6 spec 042 contract tests.** `TestComposeContract_LiveFile`, `_AdversarialLiteralBind`, `_AdversarialInfraHasPorts`, `_AdversarialMultiPortsBypass`, `_AdversarialMLMultiPortsBypass`, `_AdversarialNetworkModeHostBypass` all PASS — BUG-042-001 and BUG-042-002 regression coverage is untouched.
- [x] **No cross-package regression.** Full Go unit suite under `./smackerel.sh test unit --go` covers `internal/config/...`, `internal/auth/...`, `internal/api/...`, `internal/deploy/...` and all subpackages — zero FAIL lines. The `internal/config` package re-runs in 16.565s (uncached because of the new test).
- [x] **No production runtime code, compose, runtime config, or doc files modified.** `git status --short` shows three modifications attributable to BUG-051-001: `M scripts/commands/config.sh`, `?? scripts/commands/config_self_hosted_runtime_env_test.sh`, `?? internal/config/sst_loader_self_hosted_runtime_env_test.go`. The bug-packet artifacts under `specs/051-deployment-secret-auth-contract/bugs/BUG-051-001-self-hosted-runtime-env-mode/` document the close-out.
- [x] **Severity classification is correct.** CRITICAL — defense-in-depth bypass affecting every self-hosted bundle once the operator provides a non-default Postgres password. Spec 044 + spec 051 production-mode runtime checks (`ValidateRuntimeAuthStartup`, `Validate()` production-mode block, runtime-side dev-default DB-password rejection, PASETO signing-material requirements) all silently disabled before the fix. Surfaced as **SEC-HL-001** by the self-hosted readiness review 2026-05-13 (bubbles.system-review four-lens synthesis: devops + security + audit + spec-freshness).
- [x] **Discovery context is accurate.** Workflow mode `test-to-doc` under parent workflow `self-hosted-deploy-readiness-sprint`. Parent-expanded child mode (nested `runSubagent` blocked by G021 anti-fabrication gate; agent dispatched directly under user authorization). The four-lens system review surfaced 26 findings total; SEC-HL-001 is the highest-severity P0 critical finding.
- [x] **Spec 051 parent is unaffected.** Parent spec status remains `In Progress — full-delivery active`. The bug fixes a defense-in-depth gap that the parent spec's FR-051-005 (defense-in-depth across SST loader AND runtime startup) explicitly required but the self-hosted bundle silently bypassed via the runtime-mode mapping. The fix strengthens FR-051-005 + FR-051-001/002/003/004 (PASETO production-mode signing material) compliance for the self-hosted target without modifying any spec 051 scenario.
- [x] **Generic-only — no environment-specific content.** The `self-hosted) SMACKEREL_ENV="production"` arm is target policy (not operator topology). The deploy adapter overlay (knb) continues to own all per-target topology (real hostnames, real Tailscale identifiers, real IPs, real ufw / Caddy / systemd unit names). No real values committed in this fix.
- [x] **Build-Once Deploy-Many integrity (G079) preserved.** The fix changes the SST → bundle pipeline only. Bundles remain immutable per `(env, sourceSha)`; rollback remains pointer-swap; cosign verification + bundle hash verification + digest pinning are all unchanged. The fix gives the self-hosted bundle the correct runtime-mode signal that the spec 044 production-mode signing material checks always assumed it would have.

## Sign-off

- **Bug:** BUG-051-001
- **Workflow mode:** test-to-doc
- **Parent workflow:** self-hosted-deploy-readiness-sprint (bubbles.system-review four-lens synthesis 2026-05-13)
- **Status:** Fixed and validated
- **Decision:** SHIP_IT
