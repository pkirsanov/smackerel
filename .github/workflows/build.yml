# Build-Once Deploy-Many: build smackerel-core and smackerel-ml images, sign with cosign
# keyless, attach SBOM (syft) and SLSA provenance, generate one config bundle per
# environment (dev, test, home-lab), and publish a build manifest naming all artifacts.
#
# This workflow ENDS at registry push. It MUST NOT SSH to any deploy target, MUST NOT
# run ./smackerel.sh deploy-target, MUST NOT mutate any host state outside the runner.
# Deploy is downstream of CI on a different trust boundary (per bubbles G074).
name: build

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  workflow_dispatch:
    inputs:
      sourceSha:
        description: 'Optional explicit source SHA (defaults to github.sha)'
        required: false
      # Spec 098 — explicit RELEASE intent override for the mobile-client build.
      # Default false: a non-release run publishes a SERVER-ONLY manifest (core +
      # ml + config bundles + chrome-bridge) and SKIPS build-clients, so a missing
      # operator-private Android signing secret can never block a server deploy. A
      # tag push (refs/tags/v*) builds clients automatically; this input lets an
      # operator force a client build off a non-tag ref.
      build_clients:
        description: 'Build + sign the mobile clients and pin them in the manifest (release intent). Default false — non-release runs publish a server-only manifest.'
        required: false
        default: false
        type: boolean

permissions:
  contents: read
  packages: write
  id-token: write # required for cosign keyless

env:
  REGISTRY: ghcr.io
  IMAGE_CORE: ghcr.io/${{ github.repository_owner }}/smackerel-core
  IMAGE_ML: ghcr.io/${{ github.repository_owner }}/smackerel-ml
  BUNDLE_REGISTRY: ghcr.io/${{ github.repository_owner }}/smackerel-config-bundles
  # Spec 085 — immutable OCI artifact repo for the native Android client (AAB + APK),
  # pushed by digest and pinned in build-manifest-<sourceSha>.yaml under clients.artifacts[].
  CLIENTS_REGISTRY: ghcr.io/${{ github.repository_owner }}/smackerel-clients

jobs:
  build-images:
    runs-on: ubuntu-latest
    outputs:
      sourceSha: ${{ steps.sha.outputs.sourceSha }}
      coreDigest: ${{ steps.core.outputs.digest }}
      mlDigest: ${{ steps.ml.outputs.digest }}
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Resolve source SHA
      id: sha
      run: echo "sourceSha=${{ github.event.inputs.sourceSha || github.sha }}" >> "$GITHUB_OUTPUT"

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

    - name: Log in to GHCR
      uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push smackerel-core
      id: core
      uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
      with:
        context: .
        file: Dockerfile
        target: core
        push: true
        tags: ${{ env.IMAGE_CORE }}:${{ steps.sha.outputs.sourceSha }}
        provenance: true
        sbom: true

    - name: Build and push smackerel-ml
      id: ml
      uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
      with:
        context: ml
        file: ml/Dockerfile
        push: true
        tags: ${{ env.IMAGE_ML }}:${{ steps.sha.outputs.sourceSha }}
        provenance: true
        sbom: true

    # ─────────────────────────────────────────────────────────────────
    # Spec 047 — CI Image Vulnerability Gate
    #
    # Trivy scans EVERY image in the matrix (smackerel-core, smackerel-ml).
    # CRITICAL or HIGH findings fail the workflow BEFORE cosign signing,
    # SBOM attestation, bundle generation, or build-manifest publication.
    #
    # Threshold rationale (design.md §Drift Prevention):
    #   - CRITICAL/HIGH = block. Operators must remediate or accept-risk
    #     via a separate documented spec before bypass.
    #   - MEDIUM/LOW/UNKNOWN = report only (visible in workflow log,
    #     does NOT block). A future spec can tighten if needed.
    #
    # Drift prevention: internal/deploy/build_workflow_vuln_gate_contract_test.go
    # statically asserts that EVERY image built in this job has a matching
    # Trivy scan step that runs BEFORE the first cosign sign step. If a
    # new image is added to the matrix without a scan, the contract test
    # fails and blocks merge.
    #
    # Evidence: each scan emits a SARIF + JSON report uploaded as a
    # workflow artifact named `trivy-scan-<image>-<sha>` and referenced
    # by digest in build-manifest.yaml under attestations.vulnerabilityScan.
    # ─────────────────────────────────────────────────────────────────
    - name: Trivy vulnerability scan — smackerel-core
      # Threshold tuning per spec 047 design.md §Threshold Tuning:
      # ignore-unfixed: true blocks deployment ONLY on CRITICAL/HIGH CVEs
      # that have an upstream fix available. Advisory CVEs in the base
      # image (debian:bookworm-slim, python:3.12-slim) with no upstream
      # fix are surfaced in the SARIF artifact for visibility but do not
      # block deploy — the prior policy of blocking on unfixed advisories
      # made every CI run unrunnable as soon as a new advisory landed in
      # the Debian/PyPI vulnerability feed, with no actionable remediation
      # path. Enforced by internal/deploy/build_workflow_vuln_gate_contract_test.go
      # which fails the build if this flag is ever flipped back to false.
      id: trivy-core
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
      with:
        image-ref: ${{ env.IMAGE_CORE }}@${{ steps.core.outputs.digest }}
        format: sarif
        output: trivy-core.sarif
        severity: CRITICAL,HIGH
        exit-code: '1'
        ignore-unfixed: true
        vuln-type: os,library
        # MUST be true: trivy-action v0.36.0 entrypoint.sh unsets TRIVY_SEVERITY
        # by default when format=sarif ("Building SARIF report with all severities"),
        # which makes --exit-code=1 fire on MEDIUM/LOW findings even though severity
        # is set to CRITICAL,HIGH. With limit-severities-for-sarif: true, the action
        # honors the severity filter for both the SARIF output and the exit-code
        # computation. Spec 047 R13. Enforced by
        # internal/deploy/build_workflow_vuln_gate_contract_test.go.
        limit-severities-for-sarif: true

    - name: Trivy vulnerability scan — smackerel-ml
      # See trivy-core step above for the §Threshold Tuning rationale —
      # ignore-unfixed: true is enforced symmetrically across both images.
      id: trivy-ml
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
      with:
        image-ref: ${{ env.IMAGE_ML }}@${{ steps.ml.outputs.digest }}
        format: sarif
        output: trivy-ml.sarif
        severity: CRITICAL,HIGH
        exit-code: '1'
        ignore-unfixed: true
        vuln-type: os,library
        # See trivy-core step above for the limit-severities-for-sarif rationale.
        # Spec 047 R13.
        limit-severities-for-sarif: true

    - name: Upload Trivy scan reports as workflow artifacts
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
      with:
        name: trivy-scan-reports-${{ steps.sha.outputs.sourceSha }}
        path: |
          trivy-core.sarif
          trivy-ml.sarif
        if-no-files-found: error

    - name: Install cosign
      uses: sigstore/cosign-installer@398d4b0eeef1380460a10c8013a76f728fb906ac # v3

    - name: Cosign keyless sign — core
      env:
        COSIGN_EXPERIMENTAL: "true"
      run: |
        cosign sign --yes "${IMAGE_CORE}@${{ steps.core.outputs.digest }}"

    - name: Cosign keyless sign — ml
      env:
        COSIGN_EXPERIMENTAL: "true"
      run: |
        cosign sign --yes "${IMAGE_ML}@${{ steps.ml.outputs.digest }}"

    - name: Install syft
      uses: anchore/sbom-action/download-syft@e22c389904149dbc22b58101806040fa8d37a610 # v0

    - name: SBOM attestation — core
      env:
        COSIGN_EXPERIMENTAL: "true"
      run: |
        syft "${IMAGE_CORE}@${{ steps.core.outputs.digest }}" -o spdx-json > core.sbom.spdx.json
        cosign attest --yes --predicate core.sbom.spdx.json --type spdxjson "${IMAGE_CORE}@${{ steps.core.outputs.digest }}"

    - name: SBOM attestation — ml
      env:
        COSIGN_EXPERIMENTAL: "true"
      run: |
        syft "${IMAGE_ML}@${{ steps.ml.outputs.digest }}" -o spdx-json > ml.sbom.spdx.json
        cosign attest --yes --predicate ml.sbom.spdx.json --type spdxjson "${IMAGE_ML}@${{ steps.ml.outputs.digest }}"

    # ─────────────────────────────────────────────────────────────────
    # SLSA provenance attestation (cosign-attested, Rekor-logged)
    #
    # docker/build-push-action@v6 with `provenance: true` produces
    # BuildKit-style in-band SLSA provenance (visible in the OCI image
    # index as an `attestation-manifest`). That representation is NOT
    # discoverable via `cosign verify-attestation --type=slsaprovenance`,
    # which downstream knb adapters use as their pre-apply trust gate.
    #
    # These steps emit a minimal SLSA v0.2 predicate (predicate type
    # `https://slsa.dev/provenance/v0.2`) describing this build's
    # builder ID, source repo, source SHA, and workflow run, then
    # cosign-attest it so consumers can verify via the Rekor transparency
    # log. The source SHA appears in `materials[].digest.sha1` so the
    # knb adapter's source-SHA cross-check (preconditions.sh
    # `verify_slsa_provenance`) passes.
    #
    # Drift detector: knb/smackerel/home-lab/preconditions.sh requires
    # this predicate. Removing these steps re-orphans every downstream
    # apply.
    # ─────────────────────────────────────────────────────────────────
    - name: SLSA provenance attestation — core
      env:
        COSIGN_EXPERIMENTAL: "true"
        SOURCE_SHA: ${{ steps.sha.outputs.sourceSha }}
      run: |
        cat > core.slsa.json <<JSON
        {
          "builder": { "id": "https://github.com/${{ github.repository }}/.github/workflows/build.yml@${{ github.ref }}" },
          "buildType": "https://github.com/slsa-framework/slsa-github-generator/generic@v1",
          "invocation": {
            "configSource": {
              "uri": "git+https://github.com/${{ github.repository }}@${{ github.ref }}",
              "digest": { "sha1": "${SOURCE_SHA}" },
              "entryPoint": ".github/workflows/build.yml"
            },
            "parameters": {},
            "environment": {
              "github_run_id": "${{ github.run_id }}",
              "github_run_attempt": "${{ github.run_attempt }}"
            }
          },
          "metadata": {
            "buildInvocationId": "${{ github.run_id }}-${{ github.run_attempt }}",
            "completeness": { "parameters": true, "environment": false, "materials": false },
            "reproducible": false
          },
          "materials": [
            {
              "uri": "git+https://github.com/${{ github.repository }}",
              "digest": { "sha1": "${SOURCE_SHA}" }
            }
          ]
        }
        JSON
        cosign attest --yes --predicate core.slsa.json --type slsaprovenance "${IMAGE_CORE}@${{ steps.core.outputs.digest }}"

    - name: SLSA provenance attestation — ml
      env:
        COSIGN_EXPERIMENTAL: "true"
        SOURCE_SHA: ${{ steps.sha.outputs.sourceSha }}
      run: |
        cat > ml.slsa.json <<JSON
        {
          "builder": { "id": "https://github.com/${{ github.repository }}/.github/workflows/build.yml@${{ github.ref }}" },
          "buildType": "https://github.com/slsa-framework/slsa-github-generator/generic@v1",
          "invocation": {
            "configSource": {
              "uri": "git+https://github.com/${{ github.repository }}@${{ github.ref }}",
              "digest": { "sha1": "${SOURCE_SHA}" },
              "entryPoint": ".github/workflows/build.yml"
            },
            "parameters": {},
            "environment": {
              "github_run_id": "${{ github.run_id }}",
              "github_run_attempt": "${{ github.run_attempt }}"
            }
          },
          "metadata": {
            "buildInvocationId": "${{ github.run_id }}-${{ github.run_attempt }}",
            "completeness": { "parameters": true, "environment": false, "materials": false },
            "reproducible": false
          },
          "materials": [
            {
              "uri": "git+https://github.com/${{ github.repository }}",
              "digest": { "sha1": "${SOURCE_SHA}" }
            }
          ]
        }
        JSON
        cosign attest --yes --predicate ml.slsa.json --type slsaprovenance "${IMAGE_ML}@${{ steps.ml.outputs.digest }}"

  build-bundles:
    needs: build-images
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env: [ dev, test, home-lab ]
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Generate config bundle for ${{ matrix.env }}
      env:
        # Spec 061 SCOPE-06c — hardware-tier selector. CI uses 'cpu' for dev/test
        # (commodity GHA runners) and 'accel' for home-lab (the operator's
        # accel-tier deploy target). The runtime tier on the actual deploy host
        # is independent; this only selects which tier's interactive-model
        # parameters get baked into the per-env bundle.
        SMACKEREL_HARDWARE_TIER: ${{ matrix.env == 'home-lab' && 'accel' || 'cpu' }}
      run: |
        ./smackerel.sh config generate --env ${{ matrix.env }} --bundle --output-dir dist/config-bundles/

    - name: Verify bundle determinism
      id: bundle-sha
      env:
        SMACKEREL_HARDWARE_TIER: ${{ matrix.env == 'home-lab' && 'accel' || 'cpu' }}
      run: |
        first_sha=$(sha256sum "dist/config-bundles/config-bundle-${{ matrix.env }}-${{ needs.build-images.outputs.sourceSha }}.tar.gz" | awk '{print $1}')
        rm -rf dist/config-bundles/
        ./smackerel.sh config generate --env ${{ matrix.env }} --bundle --output-dir dist/config-bundles/
        second_sha=$(sha256sum "dist/config-bundles/config-bundle-${{ matrix.env }}-${{ needs.build-images.outputs.sourceSha }}.tar.gz" | awk '{print $1}')
        if [[ "$first_sha" != "$second_sha" ]]; then
          echo "ERROR: bundle generation is non-deterministic for env=${{ matrix.env }}" >&2
          exit 1
        fi
        echo "bundleSha=$first_sha" >> "$GITHUB_OUTPUT"

    - name: Install oras
      uses: oras-project/setup-oras@22ce207df3b08e061f537244349aac6ae1d214f6 # v1

    - name: Log in to GHCR
      run: echo "${{ secrets.GITHUB_TOKEN }}" | oras login -u ${{ github.actor }} --password-stdin ${{ env.REGISTRY }}

    - name: Push bundle to OCI registry
      run: |
        cd dist/config-bundles
        oras push "${{ env.BUNDLE_REGISTRY }}:${{ matrix.env }}-${{ needs.build-images.outputs.sourceSha }}" \
          "config-bundle-${{ matrix.env }}-${{ needs.build-images.outputs.sourceSha }}.tar.gz:application/vnd.smackerel.config-bundle.v1+gzip"

    # BUG-047-001 / DEVOPS-HL-002 — emit the per-env bundle sha256 as a build
    # artifact so the publish-build-manifest job can write it into the
    # `configBundles[*].sha256` field of build-manifest-<sourceSha>.yaml. The
    # adapter `apply.sh` MUST verify the bundle hash byte-for-byte against this
    # value before mounting the bundle (Build-Once Deploy-Many invariant; see
    # docs/Deployment.md "Missing bundle hash verification | Allows tampered
    # config"). Without this artifact + the manifest field, the operator has
    # no source of truth for the expected hash, collapsing bundle-tamper
    # defense-in-depth.
    - name: Write bundle-sha artifact for build-manifest consumption
      run: |
        mkdir -p dist/bundle-shas
        printf '%s' "${{ steps.bundle-sha.outputs.bundleSha }}" > "dist/bundle-shas/${{ matrix.env }}.sha"
        echo "bundle-sha for ${{ matrix.env }}: $(cat dist/bundle-shas/${{ matrix.env }}.sha)"

    - name: Upload bundle-sha artifact
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
      with:
        name: bundle-sha-${{ matrix.env }}-${{ needs.build-images.outputs.sourceSha }}
        path: dist/bundle-shas/${{ matrix.env }}.sha
        if-no-files-found: error

  # ─────────────────────────────────────────────────────────────────
  # Spec 058 Scope 4 — Chrome Extension Bridge build + cosign keyless
  # sign. Runs on the same source SHA as the core/ml images so the
  # extension zip is pinned by Build-Once Deploy-Many alongside the
  # server artifacts. Emits:
  #   - smackerel-chrome-bridge-<version>-<sha-short>.zip
  #   - <zip>.sha256
  #   - <zip>.sig                  (cosign keyless, Rekor-logged)
  #   - bundle-sha-chrome-bridge   (workflow artifact; consumed by
  #     publish-build-manifest to write chromeBridge.zipSha256)
  # ─────────────────────────────────────────────────────────────────
  build-chrome-bridge:
    needs: build-images
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Setup Node
      uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4
      with:
        node-version: '20'

    - name: Build chrome-bridge extension
      run: ./smackerel.sh build --extension chrome-bridge

    - name: Capture artifact metadata
      id: artifact
      run: |
        zip_path="$(ls dist/extension/smackerel-chrome-bridge-*.zip | head -1)"
        if [[ -z "$zip_path" ]]; then
          echo "ERROR: build-chrome-bridge.sh did not produce a zip" >&2
          exit 1
        fi
        zip_name="$(basename "$zip_path")"
        zip_sha="$(awk '{print $1}' "${zip_path}.sha256")"
        if ! [[ "$zip_sha" =~ ^[0-9a-f]{64}$ ]]; then
          echo "ERROR: invalid sha256 in ${zip_path}.sha256: $zip_sha" >&2
          exit 1
        fi
        echo "zip_path=$zip_path" >> "$GITHUB_OUTPUT"
        echo "zip_name=$zip_name" >> "$GITHUB_OUTPUT"
        echo "zip_sha=$zip_sha"   >> "$GITHUB_OUTPUT"

    - name: Install cosign
      uses: sigstore/cosign-installer@398d4b0eeef1380460a10c8013a76f728fb906ac # v3

    - name: Cosign keyless sign — chrome-bridge zip
      env:
        COSIGN_EXPERIMENTAL: "true"
      run: |
        cosign sign-blob --yes \
          --output-signature "${{ steps.artifact.outputs.zip_path }}.sig" \
          "${{ steps.artifact.outputs.zip_path }}"

    - name: Write chrome-bridge-sha artifact for build-manifest consumption
      run: |
        mkdir -p dist/chrome-bridge-shas
        printf '%s' "${{ steps.artifact.outputs.zip_sha }}" \
          > "dist/chrome-bridge-shas/chrome-bridge.sha"
        printf '%s' "${{ steps.artifact.outputs.zip_name }}" \
          > "dist/chrome-bridge-shas/chrome-bridge.name"

    - name: Upload chrome-bridge zip + signature
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
      with:
        name: chrome-bridge-${{ needs.build-images.outputs.sourceSha }}
        path: |
          ${{ steps.artifact.outputs.zip_path }}
          ${{ steps.artifact.outputs.zip_path }}.sha256
          ${{ steps.artifact.outputs.zip_path }}.sig
        if-no-files-found: error

    - name: Upload chrome-bridge sha artifact
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
      with:
        name: chrome-bridge-sha-${{ needs.build-images.outputs.sourceSha }}
        path: dist/chrome-bridge-shas/
        if-no-files-found: error

    - name: Attach zip + signature to GitHub Release (tag pushes only)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@72f2c25fcb47643c292f7107632f7a47c1df5cd8 # v2
      with:
        files: |
          ${{ steps.artifact.outputs.zip_path }}
          ${{ steps.artifact.outputs.zip_path }}.sha256
          ${{ steps.artifact.outputs.zip_path }}.sig

  # ─────────────────────────────────────────────────────────────────
  # Spec 085 — Client Binary Release. Build smackerel's Flutter Android client
  # ONCE per sourceSha (Build-Once Deploy-Many), reproducibly (SOURCE_DATE_EPOCH
  # = commit time; commit-derived version — never wall-clock; NFR-CBR-005),
  # distribution-sign it with the OPERATOR-PRIVATE upload keystore (GitHub secret,
  # env-ref only — never committed; Scope 03 / FR-CBR-007), cosign-keyless
  # provenance-sign each artifact (Rekor; the SAME scheme as the two images), and
  # push the AAB + APK to ${CLIENTS_REGISTRY} addressed by sha256. The per-artifact
  # digests are uploaded so publish-build-manifest pins them in
  # build-manifest-<sourceSha>.yaml under clients.artifacts[] (knb spec 025).
  #
  # The buildable Android target for the clients/mobile/assistant Flutter plugin
  # skeleton is its example app (CLIENT_APP_DIR); when a standalone app module
  # lands the path moves but the pipeline shape is identical.
  #
  # Trust boundary: this job STOPS at registry push — NO ssh/scp/rsync/apply.
  # Deploy is downstream of CI on a different trust boundary (bubbles G074).
  # Drift detector: internal/deploy/build_workflow_clients_contract_test.go.
  # ─────────────────────────────────────────────────────────────────
  build-clients:
    needs: build-images
    # Spec 098 — release-gated. The client build (and its operator-private Android
    # signing secret) runs ONLY on explicit release intent: a tag push
    # (refs/tags/v*) or an explicit `build_clients: true` workflow_dispatch. On a
    # normal push to main this job is SKIPPED, so a missing ANDROID_KEYSTORE_BASE64
    # secret can never fail this job and block the server manifest
    # (publish-build-manifest tolerates a skipped build-clients). The release path
    # is unchanged: on a tag the client is built, signed, pushed by digest, pinned.
    if: ${{ startsWith(github.ref, 'refs/tags/') || github.event.inputs.build_clients == 'true' }}
    runs-on: ubuntu-latest
    env:
      CLIENT_APP_DIR: clients/mobile/assistant/example
      FLUTTER_VERSION: "3.24.0"
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      with:
        fetch-depth: 0 # full history for git rev-list --count (commit-derived version)

    - name: Resolve reproducible build inputs
      id: repro
      env:
        SOURCE_SHA: ${{ needs.build-images.outputs.sourceSha }}
      run: |
        # Deterministic, commit-derived inputs only — never wall-clock (NFR-CBR-005).
        epoch="$(git show -s --format=%ct "$SOURCE_SHA")"
        build_number="$(git rev-list --count "$SOURCE_SHA")"
        build_name="0.1.0+${SOURCE_SHA:0:12}"
        {
          echo "sourceDateEpoch=$epoch"
          echo "buildNumber=$build_number"
          echo "buildName=$build_name"
        } >> "$GITHUB_OUTPUT"

    - name: Set up Flutter SDK (pinned, reproducible)
      run: |
        # Pin the SDK by tag from the canonical upstream (deterministic; avoids an
        # un-pinnable third-party setup action). Supply-chain: flutter/flutter only.
        git clone --depth 1 --branch "$FLUTTER_VERSION" \
          https://github.com/flutter/flutter.git "$RUNNER_TEMP/flutter"
        echo "$RUNNER_TEMP/flutter/bin" >> "$GITHUB_PATH"

    - name: Materialize Android upload keystore (operator-private secret)
      env:
        ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      run: |
        # FR-CBR-007 / NO-DEFAULTS: the distribution keystore is operator-private
        # (GitHub secret), base64-decoded into a runner-tmp path that is NEVER
        # committed and is removed at job end. Fail-fast if the secret is absent —
        # there is no unsigned fallback path.
        : "${ANDROID_KEYSTORE_BASE64:?ANDROID_KEYSTORE_BASE64 secret required — release signing has no unsigned fallback (FR-CBR-007)}"
        keystore_path="$RUNNER_TEMP/smackerel-upload.keystore"
        printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 -d > "$keystore_path"
        echo "ANDROID_KEYSTORE_PATH=$keystore_path" >> "$GITHUB_ENV"

    - name: Build Android AAB + APK (release, reproducible, distribution-signed)
      env:
        SOURCE_DATE_EPOCH: ${{ steps.repro.outputs.sourceDateEpoch }}
        ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
        ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        BUILD_NUMBER: ${{ steps.repro.outputs.buildNumber }}
        BUILD_NAME: ${{ steps.repro.outputs.buildName }}
      run: |
        # Fail-fast on any missing signing credential (NO-DEFAULTS; no unsigned path).
        : "${ANDROID_KEYSTORE_PATH:?ANDROID_KEYSTORE_PATH must be set by the keystore-materialize step}"
        : "${ANDROID_KEYSTORE_PASSWORD:?ANDROID_KEYSTORE_PASSWORD secret required}"
        : "${ANDROID_KEY_ALIAS:?ANDROID_KEY_ALIAS secret required}"
        : "${ANDROID_KEY_PASSWORD:?ANDROID_KEY_PASSWORD secret required}"
        flutter --version
        cd "$CLIENT_APP_DIR"
        flutter pub get
        flutter build appbundle --release \
          --build-number "$BUILD_NUMBER" --build-name "$BUILD_NAME"
        flutter build apk --release \
          --build-number "$BUILD_NUMBER" --build-name "$BUILD_NAME"

    - name: Install cosign
      uses: sigstore/cosign-installer@398d4b0eeef1380460a10c8013a76f728fb906ac # v3

    - name: Install oras
      uses: oras-project/setup-oras@22ce207df3b08e061f537244349aac6ae1d214f6 # v1

    - name: Log in to GHCR
      run: echo "${{ secrets.GITHUB_TOKEN }}" | oras login -u ${{ github.actor }} --password-stdin ${{ env.REGISTRY }}

    - name: Push AAB + APK to ghcr by digest
      id: push
      env:
        SOURCE_SHA: ${{ needs.build-images.outputs.sourceSha }}
      run: |
        aab="$CLIENT_APP_DIR/build/app/outputs/bundle/release/app-release.aab"
        apk="$CLIENT_APP_DIR/build/app/outputs/flutter-apk/app-release.apk"
        for f in "$aab" "$apk"; do
          [ -s "$f" ] || { echo "ERROR: expected build artifact missing/empty: $f" >&2; exit 1; }
        done
        aab_out="$(oras push "$CLIENTS_REGISTRY:android-aab-$SOURCE_SHA" \
          "$aab:application/vnd.android.package-archive")"
        apk_out="$(oras push "$CLIENTS_REGISTRY:android-apk-$SOURCE_SHA" \
          "$apk:application/vnd.android.package-archive")"
        aab_digest="$(printf '%s\n' "$aab_out" | sed -n 's/.*[Dd]igest: *sha256:\([0-9a-f]\{64\}\).*/\1/p' | head -n1)"
        apk_digest="$(printf '%s\n' "$apk_out" | sed -n 's/.*[Dd]igest: *sha256:\([0-9a-f]\{64\}\).*/\1/p' | head -n1)"
        for d in "$aab_digest" "$apk_digest"; do
          case "$d" in
            "" | *[!0-9a-f]*) echo "ERROR: failed to capture a 64-hex OCI digest: '$d'" >&2; exit 1 ;;
          esac
        done
        {
          echo "aabRef=$CLIENTS_REGISTRY@sha256:$aab_digest"
          echo "apkRef=$CLIENTS_REGISTRY@sha256:$apk_digest"
          echo "aabDigest=$aab_digest"
          echo "apkDigest=$apk_digest"
        } >> "$GITHUB_OUTPUT"

    - name: Cosign keyless provenance sign — android AAB + APK
      env:
        COSIGN_EXPERIMENTAL: "true"
        AAB_REF: ${{ steps.push.outputs.aabRef }}
        APK_REF: ${{ steps.push.outputs.apkRef }}
      run: |
        cosign sign --yes "$AAB_REF"
        cosign sign --yes "$APK_REF"

    - name: Write client-sha artifacts for build-manifest consumption
      env:
        AAB_DIGEST: ${{ steps.push.outputs.aabDigest }}
        APK_DIGEST: ${{ steps.push.outputs.apkDigest }}
      run: |
        mkdir -p dist/client-shas
        printf '%s' "$AAB_DIGEST" > dist/client-shas/android-aab.sha
        printf '%s' "$APK_DIGEST" > dist/client-shas/android-apk.sha
        echo "android AAB sha256: $AAB_DIGEST"
        echo "android APK sha256: $APK_DIGEST"

    - name: Upload client-sha artifacts
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
      with:
        name: client-shas-${{ needs.build-images.outputs.sourceSha }}
        path: dist/client-shas/
        if-no-files-found: error

    - name: Remove materialized keystore (defense-in-depth)
      if: always()
      run: rm -f "$RUNNER_TEMP/smackerel-upload.keystore"

  publish-build-manifest:
    needs: [ build-images, build-bundles, build-chrome-bridge, build-clients ]
    # Spec 098 — publish the SERVER manifest even when the mobile-client build was
    # SKIPPED (non-release push) or absent. build-clients stays in `needs` for
    # RELEASE ordering (a tagged build must finish + upload its digests before the
    # clients block is appended), but the manifest MUST NOT be gated on it: a
    # missing operator-private Android secret on a non-release push must never
    # block the core+ml server manifest. The explicit success checks for the
    # server-side jobs keep the manifest fail-closed on what actually matters; a
    # build-clients FAILURE on a release still blocks it (only success|skipped pass).
    if: >-
      ${{ !cancelled() && needs.build-images.result == 'success' && needs.build-bundles.result == 'success' && needs.build-chrome-bridge.result == 'success' && (needs.build-clients.result == 'success' || needs.build-clients.result == 'skipped') }}
    runs-on: ubuntu-latest
    steps:
    # BUG-047-001 / DEVOPS-HL-002 — pull the per-env bundle-sha artifacts that
    # build-bundles uploaded so the build manifest can declare the expected
    # bundle hash for each environment. Without these, the manifest's
    # `configBundles[*]` entries name only the OCI ref (which is mutable on
    # the registry side) and the adapter has no source of truth for hash
    # verification — collapsing the Build-Once Deploy-Many bundle-tamper gate.
    - name: Download bundle-sha artifacts
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
      with:
        pattern: bundle-sha-*-${{ needs.build-images.outputs.sourceSha }}
        path: bundle-shas
        merge-multiple: false

    - name: Download chrome-bridge sha artifact
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
      with:
        name: chrome-bridge-sha-${{ needs.build-images.outputs.sourceSha }}
        path: chrome-bridge-shas

    # Spec 085 — pull the android client digests build-clients uploaded so the
    # build manifest can pin the AAB + APK by sha256 under clients.artifacts[].
    # Spec 098 — conditional on a SUCCESSFUL build-clients (release intent only).
    # On a non-release push build-clients is skipped, this download is skipped,
    # and the manifest stays SERVER-ONLY: the android platform is NOT contracted,
    # so the knb conformance gate check (c) (E025-CLIENT-MANIFEST-NO-DIGEST) has
    # nothing to fail-close on (matching the local-build-manifest precedent). On a
    # release the digests are pinned exactly as before.
    - name: Download client-sha artifact
      if: ${{ needs.build-clients.result == 'success' }}
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
      with:
        name: client-shas-${{ needs.build-images.outputs.sourceSha }}
        path: client-shas

    - name: Resolve chrome-bridge artifact metadata
      id: resolve-chrome-bridge
      run: |
        sha_file="chrome-bridge-shas/chrome-bridge.sha"
        name_file="chrome-bridge-shas/chrome-bridge.name"
        if [[ ! -s "$sha_file" || ! -s "$name_file" ]]; then
          echo "ERROR: chrome-bridge artifact metadata missing" >&2
          exit 1
        fi
        sha_value="$(cat "$sha_file")"
        name_value="$(cat "$name_file")"
        if ! [[ "$sha_value" =~ ^[0-9a-f]{64}$ ]]; then
          echo "ERROR: chrome-bridge zip sha256 invalid: $sha_value" >&2
          exit 1
        fi
        echo "zipSha=$sha_value"   >> "$GITHUB_OUTPUT"
        echo "zipName=$name_value" >> "$GITHUB_OUTPUT"

    - name: Resolve per-env bundle hashes
      id: resolve-bundle-shas
      run: |
        # Each artifact lands at bundle-shas/<artifact-name>/<env>.sha
        # The artifact-name pattern is bundle-sha-<env>-<sourceSha>; the inner
        # file is named <env>.sha. We prefer reading by env name to be robust
        # to any layout drift in the upload step.
        for env in dev test home-lab; do
          sha_file="$(find bundle-shas -type f -name "${env}.sha" | head -1)"
          if [[ -z "${sha_file}" ]] || [[ ! -s "${sha_file}" ]]; then
            echo "ERROR: missing or empty bundle-sha file for env=${env}" >&2
            exit 1
          fi
          sha_value="$(cat "${sha_file}")"
          if ! [[ "${sha_value}" =~ ^[0-9a-f]{64}$ ]]; then
            echo "ERROR: bundle-sha for env=${env} is not a valid sha256 hex digest: ${sha_value}" >&2
            exit 1
          fi
          # Normalize env name to a shell-var-friendly form (home-lab -> HOME_LAB)
          var_name="BUNDLE_SHA_$(echo "${env}" | tr 'a-z-' 'A-Z_')"
          echo "${var_name}=${sha_value}" >> "$GITHUB_OUTPUT"
          echo "Resolved ${var_name}=${sha_value}"
        done

    # Spec 085 — validate + surface the android client digests for the clients
    # block. Fail-closed here too (defense-in-depth alongside the emitter): a
    # missing/malformed digest never reaches the manifest.
    # Spec 098 — conditional on a SUCCESSFUL build-clients (release intent only).
    - name: Resolve android client digests
      id: resolve-client-shas
      if: ${{ needs.build-clients.result == 'success' }}
      run: |
        aab_file="client-shas/android-aab.sha"
        apk_file="client-shas/android-apk.sha"
        if [[ ! -s "$aab_file" || ! -s "$apk_file" ]]; then
          echo "ERROR: missing android client digest file(s) from build-clients" >&2
          exit 1
        fi
        aab_sha="$(cat "$aab_file")"
        apk_sha="$(cat "$apk_file")"
        for d in "$aab_sha" "$apk_sha"; do
          if ! [[ "$d" =~ ^[0-9a-f]{64}$ ]]; then
            echo "ERROR: android client digest is not a 64-hex sha256: '$d'" >&2
            exit 1
          fi
        done
        {
          echo "aabSha=$aab_sha"
          echo "apkSha=$apk_sha"
        } >> "$GITHUB_OUTPUT"

    - name: Write build-manifest-${{ needs.build-images.outputs.sourceSha }}.yaml
      env:
        BUNDLE_SHA_DEV: ${{ steps.resolve-bundle-shas.outputs.BUNDLE_SHA_DEV }}
        BUNDLE_SHA_TEST: ${{ steps.resolve-bundle-shas.outputs.BUNDLE_SHA_TEST }}
        BUNDLE_SHA_HOME_LAB: ${{ steps.resolve-bundle-shas.outputs.BUNDLE_SHA_HOME_LAB }}
        CHROME_BRIDGE_ZIP_SHA: ${{ steps.resolve-chrome-bridge.outputs.zipSha }}
        CHROME_BRIDGE_ZIP_NAME: ${{ steps.resolve-chrome-bridge.outputs.zipName }}
      run: |
        cat > build-manifest.yaml <<EOF
        manifestVersion: 1
        trustModel: ci-keyless
        sourceSha: ${{ needs.build-images.outputs.sourceSha }}
        builtAt: $(date -u +%Y-%m-%dT%H:%M:%SZ)
        ciRunUrl: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        images:
          - name: smackerel-core
            ref: ${{ env.IMAGE_CORE }}@${{ needs.build-images.outputs.coreDigest }}
          - name: smackerel-ml
            ref: ${{ env.IMAGE_ML }}@${{ needs.build-images.outputs.mlDigest }}
        configBundles:
          - env: dev
            ref: ${{ env.BUNDLE_REGISTRY }}:dev-${{ needs.build-images.outputs.sourceSha }}
            sha256: ${BUNDLE_SHA_DEV}
          - env: test
            ref: ${{ env.BUNDLE_REGISTRY }}:test-${{ needs.build-images.outputs.sourceSha }}
            sha256: ${BUNDLE_SHA_TEST}
          - env: home-lab
            ref: ${{ env.BUNDLE_REGISTRY }}:home-lab-${{ needs.build-images.outputs.sourceSha }}
            sha256: ${BUNDLE_SHA_HOME_LAB}
        chromeBridge:
          artifact: ${CHROME_BRIDGE_ZIP_NAME}
          zipSha256: ${CHROME_BRIDGE_ZIP_SHA}
          signature: ${CHROME_BRIDGE_ZIP_NAME}.sig
          signatureScheme: cosign-keyless
          transparencyLog: rekor
          specReference: specs/058-chrome-extension-bridge/spec.md
        attestations:
          scheme: cosign-keyless
          transparencyLog: rekor
          sbom: spdx-json
          provenance: slsa
          vulnerabilityScan:
            scanner: trivy
            severityThreshold: CRITICAL,HIGH
            gateBlocksOn: CRITICAL,HIGH-with-upstream-fix
            ignoreUnfixed: true
            ignoreUnfixedRationale: "Threshold tuning per spec 047 design.md §Threshold Tuning. Block on FIXABLE CRITICAL/HIGH only; surface unfixed advisory CVEs in SARIF artifact for visibility without making the gate non-deployable."
            evidenceArtifact: trivy-scan-reports-${{ needs.build-images.outputs.sourceSha }}
            specReference: specs/047-ci-image-vulnerability-gate/spec.md
        EOF
        cat build-manifest.yaml

    # Spec 085 — append the clients.artifacts[] block (knb spec 025) to the SAME
    # single build-manifest-<sourceSha>.yaml. The emitter fail-closes on an
    # empty/malformed digest, so a contracted android platform always carries a
    # non-empty sha256 + cosign-keyless provenance (knb gate check c).
    # Spec 098 — conditional on a SUCCESSFUL build-clients. When clients are
    # skipped (non-release) this block is NOT appended and the manifest stays
    # server-only (android NOT contracted); when clients are built (release) the
    # block pins them by digest exactly as before.
    - name: Append clients block to build manifest (knb spec 025)
      if: ${{ needs.build-clients.result == 'success' }}
      env:
        ANDROID_AAB_SHA256: ${{ steps.resolve-client-shas.outputs.aabSha }}
        ANDROID_APK_SHA256: ${{ steps.resolve-client-shas.outputs.apkSha }}
      run: |
        bash scripts/deploy/client-manifest-clients-block.sh >> build-manifest.yaml
        cat build-manifest.yaml

    - name: Upload build manifest
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
      with:
        name: build-manifest-${{ needs.build-images.outputs.sourceSha }}
        path: build-manifest.yaml
        if-no-files-found: error
  # NOTE: this workflow stops here. Deploy is downstream of CI on a different trust boundary.
  # An operator (or a separate, manually triggered workflow) runs:
  #   ./smackerel.sh deploy-target home-lab apply --image-core=<digest> --image-ml=<digest> --config-bundle=<bundle>
  # CI MUST NOT SSH to any host. CI MUST NOT call deploy-target. (bubbles G074)
