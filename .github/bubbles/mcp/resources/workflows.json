{
  "uri": "bubbles://workflows.yaml",
  "name": "Bubbles workflows registry",
  "description": "The canonical workflow-mode + gate-block source of truth (bubbles/workflows.yaml, composed with bubbles/workflows/modes.yaml). Read this to see every mode's phase list, gate requirements, status ceiling, and template inheritance. Counts are deliberately not restated here: they moved with every release and the restated figures went stale.",
  "mimeType": "application/yaml",
  "path": "bubbles/workflows.yaml"
}
