{
  "name": "verify_status_transition",
  "description": "DEPRECATED alias for validate_dod; it runs the identical script with identical arguments and will be removed once downstream usage reaches zero. Prefer validate_dod. Retained for the v6 ABSOLUTE COMPLETION HIERARCHY framing: 'before flipping state.json status to done, run the transition guard and surface the verbatim verdict'.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "spec_dir": {
        "type": "string",
        "description": "Spec directory relative to repo root."
      }
    },
    "required": ["spec_dir"]
  },
  "script": "state-transition-guard.sh",
  "argsTemplate": ["${spec_dir}"],
  "annotations": {
    "readOnlyHint": true,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  },
  "successExitCodes": [0]
}
