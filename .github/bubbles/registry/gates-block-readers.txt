# Scripts that depend on the generated gates: block in bubbles/workflows.yaml.
#
# Generated by bubbles/scripts/gates-block-reader-lint.sh --seed and held by
# the same lint. IMP-042 SCOPE-13 permits deleting the block only once this
# list is EMPTY, because every entry breaks when the block goes.
#
# To remove an entry, repoint the script at bubbles/registry/gates.yaml and
# prove its output is byte-identical before and after.
#
bubbles/scripts/agent-ownership-lint.sh
