# Pinned production dependencies for smackerel-ml sidecar.
# Generated from pyproject.toml — update when dependencies change.
#
# spec-047 R16 (fixable-CVE family, smackerel-ml image): resolve three
# Trivy-gated CRITICAL/HIGH CVEs that began failing the build.yml
# `Trivy vulnerability scan — smackerel-ml` step (lineage: R12.2 setuptools,
# R12.3 starlette, R14 ml-OS, R15 core-openssl):
#   * CVE-2026-49468 — CRITICAL — litellm <1.84.0 (Authentication Bypass via
#     Host Header Injection): bump litellm 1.83.14 -> 1.84.0 (patch bump; the
#     advisory notes no configuration change is required).
#   * CVE-2026-48818 — HIGH — starlette <1.1.0 (StaticFiles SSRF on Windows)
#     AND CVE-2026-54283 — HIGH — starlette <1.3.1 (multipart DoS): both need
#     starlette>=1.3.1 (the higher fix). fastapi 0.115.6 capped
#     starlette>=0.40.0,<0.42.0, so bump fastapi 0.115.6 -> 0.133.0 — the
#     minimal fastapi whose spec relaxes the cap to a bare `starlette>=0.40.0`
#     (0.132.0 still pinned `<1.0.0`), permitting the starlette 1.x line — and
#     add an explicit starlette==1.3.1 pin (below) for a deterministic,
#     auditable lock. starlette 1.3.1 also supersedes the earlier
#     CVE-2024-47874 multipart-DoS fix that the old 0.115.6 comment tracked.
#     The sidecar uses only the stable lifespan / APIRouter+Depends / Request /
#     PlainTextResponse / httpx-TestClient subset (no StaticFiles, no multipart
#     Form/File) — reviewed, so no app-code change is required.
fastapi==0.133.0
# starlette is normally transitive via fastapi; pinned explicitly here so the
# CVE-2026-48818 / CVE-2026-54283 fix (>=1.3.1) is deterministic and visible to
# `trivy fs ml/requirements.txt`. Must stay >=1.3.1 and within fastapi's range.
starlette==1.3.1
uvicorn[standard]==0.32.0
nats-py==2.9.0
pydantic==2.12.5
pydantic-settings==2.6.0
httpx==0.28.1
litellm==1.84.0
# spec-047 R17 (fixable-CVE family, smackerel-ml image): resolve CVE-2026-4372
# (HIGH, fixed in 5.3.0) and CVE-2026-5241 (HIGH, arbitrary code execution via
# overridden trust_remote_code, fixed in 5.5.0). transformers is transitive via
# sentence-transformers; sentence-transformers==3.3.0 capped transformers
# <5.0.0, so bump sentence-transformers 3.3.0 -> 5.6.0 — the current release
# whose spec relaxes the cap to `transformers<6.0.0,>=4.41.0`, permitting the
# transformers 5.x line — and add an explicit transformers==5.5.0 pin (below)
# for a deterministic, auditable lock visible to the image Trivy scan. The
# sidecar uses ONLY the stable SentenceTransformer(name).encode(text,
# normalize_embeddings=True) API (ml/app/embedder.py), unchanged across the
# 3.x -> 5.x bump — reviewed, so no app-code change is required.
sentence-transformers==5.6.0
# transformers is normally transitive via sentence-transformers; pinned
# explicitly here so the CVE-2026-4372 and CVE-2026-5241 fixes (>=5.5.0) are deterministic and
# visible to `trivy fs ml/requirements.txt`. Must stay >=5.5.0 and within
# sentence-transformers' range (transformers<6.0.0,>=4.41.0).
transformers==5.5.0
prometheus_client==0.21.0
youtube-transcript-api==0.6.3
trafilatura==1.12.0
pypdf==4.1.0
numpy==1.26.0
jsonschema==4.23.0
PyYAML==6.0.2
# BUG-059-001: gkeepapi live-mode dependency (Google Keep). UNOFFICIAL,
# reverse-engineered client that Google periodically breaks — exact-pinned and
# bumped deliberately per the spec 059 drift-circuit-breaker runbook. pip
# resolves the transitive deps gpsoauth>=1.1.0 and future>=0.16.0 automatically.
gkeepapi==0.17.1
