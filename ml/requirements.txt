# Pinned production dependencies for smackerel-ml sidecar.
# Generated from pyproject.toml — update when dependencies change.
# spec-047 R12.2: bumped fastapi to 0.115.6 to pull starlette>=0.40.0,
# resolving CVE-2024-47874 (Starlette DoS via multipart/form-data).
# fastapi 0.115.6 transitively requires starlette>=0.40.0,<0.42.0.
fastapi==0.115.6
uvicorn[standard]==0.32.0
nats-py==2.9.0
pydantic==2.12.5
pydantic-settings==2.6.0
httpx==0.28.1
litellm==1.83.14
sentence-transformers==3.3.0
prometheus_client==0.21.0
youtube-transcript-api==0.6.3
trafilatura==1.12.0
pypdf==4.1.0
numpy==1.26.0
jsonschema==4.23.0
PyYAML==6.0.2
# BUG-059-001: gkeepapi live-mode dependency (Google Keep). UNOFFICIAL,
# reverse-engineered client that Google periodically breaks — exact-pinned and
# bumped deliberately per the spec 059 drift-circuit-breaker runbook. pip
# resolves the transitive deps gpsoauth>=1.1.0 and future>=0.16.0 automatically.
gkeepapi==0.17.1
