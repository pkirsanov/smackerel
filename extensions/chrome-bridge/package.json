{
  "name": "smackerel-chrome-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Smackerel Chrome MV3 extension that streams bookmarks + browser history into the smackerel-core ingest endpoint (spec 058).",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npm run build && playwright test --config test/e2e/playwright.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1",
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.12.7",
    "esbuild": "^0.21.5",
    "fake-indexeddb": "^6.0.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
